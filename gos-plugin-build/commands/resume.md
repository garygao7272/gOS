---
description: "Top-level alias for /gos resume — restore most recent saved session"
alias: true
---

# /resume — Alias for `/gos resume`

This is a convenience alias. `/resume` executes exactly the same logic as `/gos resume`. See `commands/gos.md` for the full restore flow (state.json → L1_essential → most recent session file → Story + Table + Next Move briefing).

**Why this alias exists:** resuming is the first thing you do in a fresh session. One-word access removes namespace friction. Rubric coverage is shared with the gos rubric.
