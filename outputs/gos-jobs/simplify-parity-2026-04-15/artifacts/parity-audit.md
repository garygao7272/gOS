# Parity Audit: ~/.claude vs gOS Repo

**Agent:** parity-auditor (a5320789fe31a36b6)
**Question:** Does a fresh `git clone && ./install.sh` reproduce 100% of ~/.claude/ state?

---

## Coverage Matrix (critical gaps only)

| Path | Source? | install.sh? | Status |
|---|---|---|---|
| ~/.claude/invariants.md | **NO** | **NO** | **CRITICAL** — Live has INV-G16, repo has no source |
| ~/.claude/CLAUDE.md | YES (templates/global-CLAUDE.md) | YES | ✓ |
| ~/.claude/.agents/ (49 files) | NO | NO | runtime-only (auto-generated) |
| ~/.claude/.cursor/ (63 files) | NO | NO | runtime plugin state |
| ~/.claude/.opencode/ (60 files) | NO | NO | runtime plugin state |
| ~/.claude/commands/ (8) | YES | YES | ✓ |
| ~/.claude/agents/ (20) | YES | YES | ✓ |
| ~/.claude/skills/ (15) | YES | YES | ✓ |
| ~/.claude/rules/ (46) | YES | YES | ✓ |
| ~/.claude/hooks/ (30) | **PARTIAL** (18 in gos-v4.3) | **NO** — reads wrong path | **CRITICAL** — 12 live hooks missing |
| ~/.claude/claws/ (3) | YES (gos-v4.3/claws) | **NO** | **CRITICAL** — Claws not installed |
| ~/.claude/settings.json | YES | YES (merge) | ✓ (Python merge fragile) |
| ~/.claude/config/ | YES | YES (setup.sh only) | ⚠ |

---

## install.sh Defects

| Issue | Severity | Line(s) |
|---|---|---|
| Does NOT copy hooks from gOS/.claude/hooks | **CRITICAL** | 248–253: reads from `.claude/hooks/` which doesn't exist in --bootstrap mode |
| Does NOT copy claws/ at all | **CRITICAL** | Missing entirely |
| Does NOT copy invariants.md | **CRITICAL** | Missing |
| settings.json merge uses Python 3; fails silently | MEDIUM | 276–280: fallback exists but loses custom hooks |
| No idempotence guarantee for --global re-runs | MEDIUM | Overwrites without checking |
| No install manifest | MEDIUM | Only counts; hard to verify completeness |

---

## Missing Live Hooks (12 not in repo)

`accumulate.sh`, `claw-runner.sh`, `delete-guard.sh`, `error-tracker.sh`, `file-changed-drift.sh`, `freeze-guard.sh`, `git-safety.sh`, `health-gate.sh`, `loop-detect.sh`, `middleware-chain.sh`, `observe.sh`, `permission-denied-handler.sh`, `phase-gate.sh`, `read-tracker.sh`, `scope-guard.sh`, `spec-compliance.sh`

(Note: some live hooks ARE in gOS/.claude/hooks/ — the repo has them, but install.sh reads from the wrong location.)

---

## Fresh-Clone Verdict: **~70% reproducibility**

If Gary runs `./install.sh --global && ./install.sh --bootstrap ~/Arx` on fresh Mac:
- ✓ Commands, agents, skills, rules installed
- ✓ settings.json merged (mostly)
- ✗ Hooks incomplete — 12 live hooks missing
- ✗ Claws not installed — source-monitor, spec-drift, market-regime absent
- ✗ **invariants.md not created** — fresh machine gets no invariants (INV-G01..G16 lost)
- ✗ Manual post-install steps required — firecrawl, gh auth, plugin installs, API keys
- ⚠ Python merge fragile — silent failure reverts to template

---

## Top 5 Fixes to 100% Parity

1. **Copy hooks correctly in install.sh** — Change line 248 to copy from `$GOS_DIR/.claude/hooks/` instead of `.claude/hooks/`. **HIGH effort, medium risk**. Fixes 12 missing hooks.

2. **Add claws/ to install.sh --global** — Lines 220–228 handle skills; add similar block for `claws/` from `gos-v4.3/claws/` (or move claws to top-level). **LOW effort, low risk**.

3. **Add invariants.md copy to install.sh** — After CLAUDE.md (line 199), copy `gOS/invariants.md` → `~/.claude/invariants.md`. **LOW effort, low risk, HIGHEST PRIORITY**.

4. **Document runtime-only dirs** — `.agents/`, `.cursor/`, `.opencode/` are auto-generated by Claude Code; add to `.gitignore` or README section.

5. **Add --check mode + install manifest** — Warn about missing Firecrawl, YouTube tools, plugins. Output installed-files count. Prevents silent failures.

---

## File Paths Cited

- `gOS/install.sh` (lines 248, 220–253)
- `gOS/.claude/hooks/` (30 hooks, install.sh reads from wrong location)
- `~/.claude/invariants.md` (no source in repo — CRITICAL)
- `gOS/gos-v4.3/claws/` (not copied by install.sh)
