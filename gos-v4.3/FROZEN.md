# FROZEN — Do Not Sync

This directory is a point-in-time snapshot of gOS v4.3. It is NOT synced from `commands/`.

**Canonical source:** `gOS/commands/`
**Active install target:** `~/.claude/commands/` (via `install.sh --global`)

Do not edit files here. They serve as a historical reference only.

Frozen: 2026-04-09
