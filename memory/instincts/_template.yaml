# Instinct Template — do not modify, copy for new instincts
# See INDEX below for active instincts.
id: inst-000
type: pattern  # pattern | preference | antipattern | hotspot
confidence: 0.3  # 0.3 (new) to 0.9 (validated)
source: observations.jsonl analysis
created: 2026-04-09
last_seen: 2026-04-09
occurrences: 1
description: "Short description of the observed pattern"
evidence:
  - session: session-id
    timestamp: "2026-04-09T00:00:00Z"
    context: "What happened that triggered this instinct"
promotes_to: null  # skill | memory | hook — filled when promoted
promoted_at: null

# ── INDEX (active instincts) ──
# no-system-jargon.yaml    | 0.9 | applied | Never show system internals in user-facing output
# audit-existing-tools.yaml | 0.7 | applied | Check installed tools before recommending new ones
# context-limit-awareness.yaml | 0.8 | applied | Large writes at high context fail — dispatch as agent
